MEMORY
{
	RAM:			start=$2100, size=$60000;
}

SEGMENTS
{
	MAIN:			load=RAM, start=$2100;
	PALETTE:		load=RAM, start=$c000;
	SPRITES:		load=RAM, start=$50000;
	SONG:			load=RAM, start=$52000;
}
